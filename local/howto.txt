# First this
JEKYLL_ENV=production jekyll build

# Then this to test
jekyll serve

# If it fails, make sure you're in the root directory of the site

# Then git add .; git commit -m "message"; git push origin master
